---
description: windows/install.ps1 基于 OpenClaw 二次开发时的增量解冻原则（禁止擅自删原代码）
globs: windows/install.ps1
alwaysApply: false
---

# Windows 安装脚本二次开发（增量解冻）

`windows/install.ps1` 由 OpenClaw 官方 `install.ps1` 二次开发而来。改动时须按模块**逐个击破**，而非一次性大改。

## 工作流程

1. **一次只动一个模块**：顺序与脚本内步骤一致：`0 前置` → `1 Node.js` → `2 Git` → `3 OpenClaw` → `4 收尾`（若脚本内编号有调整，以当前脚本为准）。
2. **专注某一模块时**：其余步骤对应的代码**全部注释掉**（优先使用 `<# ... #>` 或行注释保留整块），保证当前模块可单独运行与验证。
3. **当前模块验收通过后**：解除该模块相关注释，进入下一模块；重复「其余注释、只改一块」直到全部模块改完。
4. **全部完成后**：解除所有因本流程临时添加的注释块，恢复完整安装路径。

## 核心约束

- **禁止在无明确要求的情况下删除原始代码**；需要暂时禁用某段逻辑时，用注释保留全文。
- 仅当用户**明确规定**可以删除某段时，才允许从文件中移除对应代码。

## 反面示例

- 为「清爽」删掉尚未改到的 Git/OpenClaw 整段函数定义。
- 在未完成全流程前，把大块注释当作永久删除处理。

## 正面示例

- 正在改 `0 前置`：步骤 `1`～`4` 整块包在 `<# ... #>`，`Main` 只保留前置逻辑。
- `0` 改完并测过后：解开 `1`，`2`～`4` 仍注释，专注 Node 相关改动。
